# vim:ft=ansible:
---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - { role: homebrew }  # install homebrew & homebrew_cask packages
    - { role: macos }     # setting by macos's defaults command
    - { role: gem }

    #- { role: dein }
    #- { role: zplug }

